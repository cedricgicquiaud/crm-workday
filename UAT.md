# Cahier de recette — crm-workday

Une section par feature, ajoutée à son cadrage. Chaque ligne est un constat observable,
coché à la recette humaine.

## Feature 1 — L'équipe entre dans le CRM

Chaque livraison remplit uniquement sa sous-section. Les titres et lignes d'introduction ne bougent pas.

### 1.1 Première page en ligne

Recette du socle, sur un clone frais avec Docker démarré.

- [ ] `docker compose up -d`, `cp .env.example .env.local`, `npm ci`, `npm run db:migrate`, `npm test` : tout passe au vert.
- [ ] `npm run dev` puis `http://localhost:3000/` : renvoie vers la page de connexion.
- [ ] `http://localhost:3000/api/health` : `status ok`, `version` égale à celle de `package.json`, `commit` égal à `git rev-parse --short HEAD`.
- [ ] Sans `DATABASE_URL` dans `.env.local`, `npm run db:migrate` s'arrête en nommant la variable.
- [ ] `npm run test:e2e` passe au vert.

### 1.2a Connexion, invitation, réinitialisation

Préparation : `npm run db:migrate`, puis `npm run seed:admin -- --email admin@exemple.fr --prenom Cédric --nom Recette --mot-de-passe 'MotDePasse-Recette-1'`, puis `npm run dev`. Les emails ne partent pas : ils sont lus dans la table `email_log` (colonne `body`, le lien y figure) ou par `npx tsx e2e/fixtures/auth.ts last-email <adresse>`.

- [ ] Relancer la même commande `npm run seed:admin` : elle s'arrête avec « un administrateur existe déjà » et ne crée rien (contrat 20).
- [ ] `npm run seed:admin -- --email x@exemple.fr` sans les autres arguments s'arrête en nommant les champs manquants.
- [ ] `http://localhost:3000/parametres/journal` sans être connecté renvoie vers `/connexion?next=%2Fparametres%2Fjournal` ; `http://localhost:3000/api/health` et `/connexion` restent servis sans session (contrat 19).
- [ ] Sur `/connexion`, saisir `personne@exemple.fr` / `MotDePasse-Faux-1` puis `admin@exemple.fr` / `MotDePasse-Faux-1` : les deux affichent exactement « Email ou mot de passe incorrect. » (contrat 14).
- [ ] Saisir `admin@exemple.fr` / `MotDePasse-Recette-1` depuis l'écran précédent (arrivé avec `?next=`) : la page `Paramètres > Journal` demandée s'ouvre ; `/accueil` affiche « Bonjour Cédric » (contrat 6).
- [ ] Se déconnecter (supprimer le cookie `better-auth.session_token` dans le navigateur), puis cinq fois `admin@exemple.fr` / `MotDePasse-Faux-1`, puis une sixième fois avec le bon mot de passe `MotDePasse-Recette-1` : refus avec le même message « Email ou mot de passe incorrect. » ; 15 minutes plus tard, le bon mot de passe passe (contrat 15).
- [ ] Session glissante (contrat 8) : connecté, dans la table `session`, mettre `expires_at` à demain et `created_at` / `updated_at` à il y a 29 jours ; recharger `/accueil` : toujours connecté, `expires_at` est repoussé à dans 30 jours. Mettre `expires_at` à hier ; recharger : renvoi vers `/connexion`.
- [ ] Invitation : connecté en administrateur, `curl -X POST http://localhost:3000/api/invitations -H 'content-type: application/json' -H 'cookie: better-auth.session_token=<valeur du cookie>' -d '{"email":"invite@exemple.fr","firstName":"Inès","lastName":"Roux","role":"membre"}'` répond `201` ; la table `user` a `invite@exemple.fr` à l'état `invite` ; `email_log` contient un email « Votre accès au CRM de votre cabinet » avec un lien `/invitation/<jeton>` (D7).
- [ ] Le même appel avec le cookie d'un membre répond `403` ; sans cookie, `401` (contrat 16, préparé).
- [ ] Le même appel avec `admin@exemple.fr` répond `409` avec `"status":"actif"` (D12).
- [ ] Ouvrir le lien `/invitation/<jeton>` dans une fenêtre privée : « Choisissez votre mot de passe ». Saisir `Court-Mdp-1` deux fois : « Le mot de passe doit contenir 12 caractères au moins. » (contrat 13). Saisir `MotDePasse-Invite-1` deux fois : arrivée sur `/accueil`, « Bonjour Inès » (contrat 6).
- [ ] Rouvrir le même lien `/invitation/<jeton>` (fenêtre privée neuve) : « Lien invalide », aucun formulaire (contrat 12). Idem après avoir mis `expires_at` de la ligne `invitation` à hier pour une invitation non utilisée.
- [ ] `curl -X POST http://localhost:3000/api/invitations/renvoyer … -d '{"email":"<un invité non encore activé>"}'` en administrateur : un nouvel email part avec un lien neuf ; l'ancien lien affiche « Lien invalide » (D7).
- [ ] `/connexion` > « Mot de passe oublié ? » > `admin@exemple.fr` : « Si un compte existe pour cette adresse, un email vient de partir. » ; `email_log` contient « Réinitialisation de votre mot de passe » avec un lien `/reinitialisation/<jeton>` (contrat 9).
- [ ] Même écran avec `personne@exemple.fr`, puis avec un compte passé à `status = 'desactive'` dans la table `user` : même phrase, aucune ligne nouvelle dans `email_log` (contrat 14).
- [ ] Suivre le lien `/reinitialisation/<jeton>` : « Choisissez un nouveau mot de passe » ; saisir `MotDePasse-Recette-2` deux fois : retour sur `/connexion` avec « Votre mot de passe a été modifié. Connectez-vous. » ; `MotDePasse-Recette-1` est refusé, `MotDePasse-Recette-2` connecte (contrat 9).
- [ ] Rouvrir le même lien `/reinitialisation/<jeton>` : « Lien invalide » (contrat 12). Un lien de plus d'une heure (`expires_at` à hier dans la table `verification`) : idem.
- [ ] Un compte à `status = 'desactive'` avec un bon mot de passe : « Email ou mot de passe incorrect. » (D12).
- [ ] Écrans `/connexion`, `/reinitialisation`, `/reinitialisation/<jeton>`, `/invitation/<jeton>` à 375 px : aucun défilement horizontal, un seul titre `<h1>`.

### 1.2b Gestion des comptes et profil

Préparation : base migrée et `admin@exemple.fr` / `MotDePasse-Recette-1` créé (voir 1.2a), puis `npm run dev`. Écrans : `http://localhost:3000/parametres/comptes` (administrateurs) et `http://localhost:3000/profil`. Les emails ne partent pas : lire `email_log` ou `npx tsx e2e/fixtures/auth.ts last-email <adresse>`.

- [ ] Connecté en administrateur, ouvrir `/parametres/comptes` : le titre « Comptes » et un tableau (Nom, Email, Rôle, État) ; `admin@exemple.fr` y figure en « Administrateur » avec un badge « Actif » (point plein, teinte succès). Un seul bouton plein sur l'écran : « Inviter ».
- [ ] Cliquer « Inviter », saisir `ines.roux@exemple.fr`, prénom `Inès`, nom `Roux`, rôle « Membre », puis « Envoyer l'invitation » : le dialogue se ferme, la ligne « Invitation envoyée à ines.roux@exemple.fr. » s'affiche, la ligne « Inès Roux » apparaît avec le badge « Invité » (teinte avertissement) ; `email_log` contient « Votre accès au CRM de votre cabinet » avec un lien `/invitation/<jeton 1>` (contrat 5).
- [ ] Ouvrir le lien `/invitation/<jeton 1>` dans une fenêtre privée : « Choisissez votre mot de passe ». Ne rien saisir, revenir à la liste.
- [ ] Sur la ligne d'Inès, menu « Actions » (bouton à trois points) > « Renvoyer l'invitation » : « Invitation renvoyée à ines.roux@exemple.fr. » ; un nouvel email contient un lien `/invitation/<jeton 2>` différent. En fenêtre privée, `<jeton 1>` affiche « Lien invalide » sans formulaire ; `<jeton 2>` affiche « Choisissez votre mot de passe » (contrat 7). Choisir `MotDePasse-Ines-1` deux fois : arrivée sur `/accueil`, « Bonjour Inès » ; dans la liste, Inès passe « Actif ».
- [ ] « Inviter » avec l'email `admin@exemple.fr` (prénom et nom quelconques) : refus dans le dialogue avec le message « Un compte existe déjà pour admin@exemple.fr : Cédric Recette (actif). », aucun bouton « Réactiver ce compte », aucune ligne nouvelle (contrat 18).
- [ ] « Inviter » avec un email mal formé (`pas-un-email`) et un prénom vide : le dialogue reste ouvert, aucun encadré global ; sous le champ Email, « Cet email n'est pas valide. », sous le champ Prénom, « Le prénom est requis. » (11 px, couleur danger, champ bordé en rouge) ; le premier champ fautif reçoit le focus. Email vidé : « L'email est requis. ».
- [ ] Menu « Actions » d'Inès > « Désactiver » : « Compte de Inès Roux désactivé. », badge « Désactivé » (gris, point creux). Dans la fenêtre privée où Inès est connectée, recharger `/accueil` : renvoi vers `/connexion`. Saisir `ines.roux@exemple.fr` / `MotDePasse-Ines-1` : « Email ou mot de passe incorrect. » (contrat 10). Dans la table `session`, plus aucune ligne pour Inès.
- [ ] « Inviter » de nouveau avec `ines.roux@exemple.fr` : message « Un compte existe déjà pour ines.roux@exemple.fr : Inès Roux (désactivé). » et un bouton « Réactiver ce compte » ; le cliquer : le dialogue se ferme, « Compte de Inès Roux réactivé. », badge « Actif ». Inès se reconnecte avec `MotDePasse-Ines-1` (contrat 10, D12). Le menu « Actions » d'un compte désactivé propose aussi « Réactiver ».
- [ ] Inès connectée dans deux navigateurs (ou une fenêtre normale et une privée). Menu « Actions » d'Inès > « Fermer toutes les sessions » : « Sessions de Inès Roux fermées : il devra se reconnecter. » ; recharger chacun des deux navigateurs : renvoi vers `/connexion` ; `MotDePasse-Ines-1` reconnecte, le badge reste « Actif » (contrat 11).
- [ ] Menu « Actions » d'Inès > « Passer administrateur » : « Inès Roux est maintenant administrateur. », colonne Rôle « Administrateur » ; puis « Passer membre » : retour à « Membre » (D11).
- [ ] Dernier administrateur (contrat 17) : avec `admin@exemple.fr` seul administrateur actif, son menu « Actions » montre « Passer membre » et « Désactiver » grisés, sous-titrés « Dernier administrateur actif » ; les cliquer ne fait rien. Avec le cookie d'un administrateur : `curl -X PATCH http://localhost:3000/api/accounts/<id de admin@exemple.fr> -H 'content-type: application/json' -H 'cookie: better-auth.session_token=<valeur>' -d '{"status":"desactive"}'` répond `409` avec `"error":"dernier_administrateur"` ; idem avec `-d '{"role":"membre"}'`. Passer Inès administrateur : les deux entrées du menu de `admin@exemple.fr` redeviennent actives.
- [ ] Connecté en membre (Inès repassée membre), ouvrir `http://localhost:3000/parametres/comptes` : renvoi vers `/accueil`. Avec son cookie, `curl http://localhost:3000/api/accounts -H 'cookie: …'` répond `403` `"error":"reserve_aux_administrateurs"` ; sans cookie, `401`. `/parametres/journal` s'ouvre pour elle (contrat 16).
- [ ] Toujours en membre, `curl -X POST http://localhost:3000/api/accounts …`, `curl -X PATCH http://localhost:3000/api/accounts/<id> …` et `curl -X DELETE http://localhost:3000/api/accounts/<id>/sessions …` répondent tous `403` (contrat 16).
- [ ] Inès ouvre `http://localhost:3000/profil` : « Mon profil », formulaire « Identité » pré-rempli (Prénom `Inès`, Nom `Roux`, email affiché non modifiable). Remplacer le prénom par `Inés`, « Enregistrer » : « Profil enregistré. » ; `/accueil` affiche « Bonjour Inés » (CRM-21). Prénom vidé puis « Enregistrer » : « Le prénom et le nom sont requis. ».
- [ ] Sur `/profil`, formulaire « Mot de passe » : mot de passe actuel `MotDePasse-Ines-1`, nouveau `Court-Mdp-1` deux fois, « Changer le mot de passe » : « Le mot de passe doit contenir 12 caractères au moins. » sous le champ « Nouveau mot de passe », bordé en rouge, sans encadré global (contrat 13). Nouveau `MotDePasse-Ines-2` et confirmation `MotDePasse-Ines-3` : « Les deux mots de passe ne sont pas identiques. » sous la confirmation. Actuel `MotDePasse-Faux-1`, nouveau et confirmation `MotDePasse-Ines-2` : « Le mot de passe actuel est incorrect. » sous « Mot de passe actuel ».
- [ ] Actuel `MotDePasse-Ines-1`, nouveau et confirmation `MotDePasse-Ines-2` : « Mot de passe modifié. », les champs se vident. Se déconnecter (supprimer le cookie) : `MotDePasse-Ines-1` est refusé, `MotDePasse-Ines-2` connecte.
- [ ] Écrans `/parametres/comptes` (liste et dialogue « Inviter » ouvert) et `/profil` à 375 px : aucun défilement horizontal de la page (le tableau défile dans son cadre), un seul titre `<h1>` (« Paramètres », « Mon profil »).

### 1.4 Emails sortants

Préparation : `npm run db:migrate` (ajoute `cabinet_settings` et `email_template`), `admin@exemple.fr` / `MotDePasse-Recette-1` créé (voir 1.2a), un membre actif (Inès, voir 1.2b), puis `npm run dev`. Écrans : `http://localhost:3000/parametres/cabinet`, `/parametres/modeles`, `/parametres/journal`, `/parametres/envoi-test`. En développement rien ne part : les emails sont « capturés » et se lisent dans le journal ou par `npx tsx e2e/fixtures/auth.ts last-email <adresse>`. Le titre de page `<h1>` est « Paramètres » ; chaque écran a son titre `<h2>`.

- [ ] Connecté en membre (Inès), ouvrir `/parametres/cabinet`, `/parametres/modeles` et `/parametres/envoi-test` : chacun renvoie vers `/accueil`. Avec son cookie, `curl http://localhost:3000/api/cabinet -H 'cookie: better-auth.session_token=<valeur>'`, `curl http://localhost:3000/api/emails/modeles …` et `curl -X POST http://localhost:3000/api/emails/test -H 'content-type: application/json' -d '{}' …` répondent `403` ; sans cookie, `401`. `/parametres/journal` s'ouvre pour elle, sans bouton « Renvoyer » (contrat 16).
- [ ] Connecté en administrateur, `/parametres/cabinet` : titre « Cabinet », trois champs (Nom du cabinet, Nom d'affichage de l'expéditeur, Adresse d'expédition), un seul bouton plein « Enregistrer ». Saisir `Cabinet Martin`, `Cabinet Martin`, `pas-une-adresse`, « Enregistrer » : sous le champ Adresse, « Cette adresse n'est pas valide. » (11 px, couleur danger, champ bordé en rouge), rien n'est enregistré. Vider le nom : « Le nom du cabinet est requis. » sous le champ.
- [ ] Saisir `contact@cabinet-martin.fr` et « Enregistrer » : « Paramètres du cabinet enregistrés. » ; recharger : les trois valeurs sont relues. La table `cabinet_settings` a une seule ligne (`id = cabinet`), même après un second enregistrement (D20).
- [ ] Inviter `paul.durand@exemple.fr` depuis `/parametres/comptes` : `email_log` contient « Votre accès au CRM de Cabinet Martin » et le corps dit « … sur le CRM de Cabinet Martin » ; « votre cabinet » n'apparaît plus (contrat 27). Dans le journal, la ligne porte Paul, le sujet, « Invitation », la date, « Capturé », l'auteur « Cédric Recette » (contrat 29).
- [ ] `/parametres/modeles` : titre « Modèles d'emails », liste de deux modèles « Invitation » et « Réinitialisation », badge « Système », sujet en sous-titre, aucun bouton « Supprimer ». Avec le cookie d'administrateur, `curl -X DELETE http://localhost:3000/api/emails/modeles/invitation …` répond `409` `"error":"modele_systeme"` ; les deux modèles sont toujours là (contrat 32). La table `email_template` a deux lignes, `required_variables = {lien}`.
- [ ] « Modifier » sur Invitation : formulaire « Modèle Invitation » avec Sujet et Corps pré-remplis, la liste « Variables disponibles » (`{{prenom}}`, `{{nom}}`, `{{cabinet}}`, `{{lien}}` — obligatoire) et un cadre « Aperçu » avec le sujet et l'email rendu (valeurs d'exemple : Ana Martin, Cabinet Martin puisque le cabinet est enregistré). Un seul bouton plein : « Enregistrer ».
- [ ] Remplacer le corps par `Bonjour {{prenom}} {{nom}},` puis une ligne vide, `Bienvenue au CRM de {{cabinet}}.`, une ligne vide, `[Ouvrir mon accès]({{lien}})` : sans enregistrer, l'aperçu se met à jour en moins d'une seconde avec « Bonjour Ana Martin, », « Bienvenue au CRM de Cabinet Martin. » et un bouton « Ouvrir mon accès » (contrat 28).
- [ ] Corps `Bonjour {{prénom}},` (accent) puis une ligne vide et `[Ouvrir]({{lien}})`, « Enregistrer » : sous le champ Corps, « La variable {{prénom}} n'existe pas. Variables disponibles : {{prenom}}, {{nom}}, {{cabinet}}, {{lien}}. », le champ est bordé en rouge, rien n'est enregistré. Idem avec `{{societe}}` dans le sujet : le message nomme `{{societe}}`, sous le champ Sujet (contrat 31).
- [ ] Corps `Bonjour {{prenom}},` seul, « Enregistrer » : « Ce modèle doit contenir la variable {{lien}}. » sous le champ Corps, rien n'est enregistré (contrat 32). Même refus sur « Réinitialisation » sans `{{lien}}`.
- [ ] Sujet `Bienvenue chez {{cabinet}}, {{prenom}}` et le corps valide de l'étape « aperçu », « Enregistrer » : « Modèle « Invitation » enregistré. » ; la liste montre le nouveau sujet. Inviter `lea.morel@exemple.fr` : l'email a pour sujet « Bienvenue chez Cabinet Martin, Léa » et le bouton « Ouvrir mon accès » mène à `/invitation/<jeton>` qui fonctionne (contrat 28). Remettre ensuite le texte d'origine si souhaité : la suite de la recette n'en dépend pas.
- [ ] `/parametres/journal` : titre « Journal des envois », filtres (Statut, Du, Au, Type d'objet, Identifiant, « Filtrer », « Effacer les filtres »), tableau Date / Destinataire / Sujet / Modèle / Statut / Auteur / Objet, pied « N envois ». Date au format `5 sept. 2026, 10:32` (Europe/Paris). Les invitations portent l'auteur « Cédric Recette » et l'objet `user <id>` ; un email de « Mot de passe oublié ? » (depuis `/connexion`, adresse d'Inès) porte l'auteur « Système » et le modèle « Réinitialisation » (contrat 29, D23).
- [ ] Statut « Échec » puis « Filtrer » : aucune ligne, « Aucun envoi ne correspond aux filtres. ». Statut « Capturé » : toutes les lignes. « Du » à demain : aucune ligne. Type d'objet `user` : seules les invitations et réinitialisations. « Effacer les filtres » : tout revient.
- [ ] Poser un échec comme l'aurait fait un refus de Resend, en SQL sur la base `crm` : `INSERT INTO email_log ("to", subject, body, template, status, error_reason) VALUES ('echec@exemple.fr', 'Relance', '<p>Relance</p>', 'test', 'echec', 'Domaine non vérifié');`. Recharger le journal : la ligne porte le badge « Échec » (teinte danger), le motif « Domaine non vérifié » à côté, et un bouton « Renvoyer ». Le cliquer : « Email renvoyé à echec@exemple.fr. » et une nouvelle ligne « Capturé » avec le même sujet, auteur « Cédric Recette » ; l'ancienne ligne reste en échec (contrat 29, D24).
- [ ] « Renvoyer » sur la ligne d'invitation de Léa (compte encore invité) : un nouvel email part avec un lien `/invitation/<jeton 2>` différent ; l'ancien lien affiche « Lien invalide ». « Renvoyer » sur la ligne de réinitialisation d'Inès : un nouvel email « Réinitialisation de votre mot de passe » avec un lien neuf (D24).
- [ ] `/parametres/envoi-test` : titre « Envoi de test », champ « Destinataire » pré-rempli avec `admin@exemple.fr`, un seul bouton plein « Envoyer l'email de test ». Saisir `pas-une-adresse` et envoyer : sous le champ, « Cette adresse n'est pas valide. », champ bordé en rouge, aucune ligne nouvelle dans `email_log` (contrat 35). Avec le cookie d'administrateur, `curl -X POST http://localhost:3000/api/emails/test -H 'content-type: application/json' -d '{"to":"pas-une-adresse"}' …` répond `400` `"error":"destinataire_invalide"`.
- [ ] Remettre `admin@exemple.fr` et envoyer : « Email de test capturé pour admin@exemple.fr : en développement, rien ne part. Il est lisible dans le journal. » ; le journal a une ligne « Email de test », « Capturé », sujet « Email de test du CRM de Cabinet Martin », corps « Bonjour Cédric, … » (contrats 30, 34). Aucune requête vers `api.resend.com` dans les journaux du serveur.
- [ ] Recette avec la vraie clé (contrat 30, D25) : sur un serveur avec `NODE_ENV=production` et `RESEND_API_KEY` valide, adresse d'expédition sur le domaine vérifié (CRM-1), « Envoyer l'email de test » affiche « Email de test envoyé à <adresse>. Vérifiez votre boîte de réception. » ; l'email arrive de « Cabinet Martin <contact@…> » ; le journal porte « Envoyé » et `provider_id` est l'identifiant Resend (CRM-26).
- [ ] Toujours en production, vider `cabinet_settings` (`DELETE FROM cabinet_settings;`) puis envoyer un email de test : « Envoi impossible : expéditeur non configuré. L'échec est consigné dans le journal. » ; la ligne du journal est « Échec » avec le motif « expéditeur non configuré » (contrat 33). Avec une adresse d'expédition sur un domaine non vérifié : « Échec » avec le motif renvoyé par Resend, une seule tentative, aucune relance automatique (contrat 29, D24).
- [ ] Écrans `/parametres/cabinet`, `/parametres/modeles` (éditeur ouvert), `/parametres/journal` et `/parametres/envoi-test` à 375 px : aucun défilement horizontal, un seul titre `<h1>` (« Paramètres »), le bouton principal de chaque écran visible sans défiler ; le journal est empilé (une carte par envoi) au lieu du tableau (contrat 25).

### 1.3 Coque de navigation

Préparation : base migrée, `admin@exemple.fr` / `MotDePasse-Recette-1` créé (voir 1.2a), un membre actif (Inès, voir 1.2b), puis `npm run dev`. Écrans : `http://localhost:3000/accueil`, `/profil`, `/parametres` et ses sections. Sur Mac, ⌘K ; sur Windows ou Linux, Ctrl+K. Le HTML servi se lit avec `curl -s http://localhost:3000/accueil -H 'cookie: better-auth.session_token=<valeur du cookie>' | head -c 300`.

- [ ] Connecté en administrateur, ouvrir `/accueil` : à gauche, une barre latérale de 224 px avec « CRM Workday », les liens Accueil, Paramètres, Mon profil, une section « Objets » avec le texte « Clients, consultants, missions et factures arrivent avec les features suivantes. », et en pied « Passer en thème sombre », « Se déconnecter », puis « Cédric Recette · Administrateur ». Accueil est surligné (fond violet pâle). En haut, une barre de 40 px avec le bouton de la barre latérale à gauche et « Rechercher ⌘K » à droite ; aucun autre titre que « Bonjour Cédric » (contrat 21).
- [ ] La date sous « Bonjour Cédric » est courte : « 5 sept. 2026 » (jour, mois abrégé, année), pas « vendredi 5 septembre 2026 » (retour de recette 1.2a).
- [ ] Cliquer le bouton de la barre latérale (icône de panneau, info-bulle « Replier ou déployer la barre latérale ») : la barre se replie à 48 px, seules les icônes restent, chacune avec une info-bulle au survol. Cliquer « Mon profil » (icône) puis recharger la page : la barre reste repliée. Dans les cookies du navigateur, `sidebar_state` vaut `false`. La déployer de nouveau : le cookie vaut `true` (contrat 21).
- [ ] Sur `/accueil`, appuyer sur ⌘K (Ctrl+K) : une palette s'ouvre au tiers de l'écran, champ « Rechercher une page ou une action… », sections « Navigation » (Aller à Accueil, Aller à Paramètres, Aller à Mon profil) puis « Actions » (Basculer le thème clair / sombre, Se déconnecter), première ligne présélectionnée, pied « ↑↓ naviguer · ↵ ouvrir · Échap fermer ». Taper `para` : seule « Aller à Paramètres » reste, présélectionnée ; Entrée ouvre `/parametres/comptes` (première section permise à un administrateur) et la palette se ferme (contrat 22).
- [ ] ⌘K puis taper `sombre` et Entrée : l'écran passe en sombre immédiatement, sans rechargement. Recharger : toujours sombre. ⌘K, `clair`, Entrée : retour au clair, recharger : toujours clair (contrat 22). Le bouton « Rechercher ⌘K » de la barre supérieure ouvre la même palette ; Échap la ferme.
- [ ] Refus : taper `zzz` dans la palette : « Aucun résultat. ». Ouvrir le fichier `src/features/shell/palette/palette.tsx` : il ne contient aucune entrée (ni « Accueil », ni « thème », ni « déconnecter ») ; les entrées vivent dans `src/features/shell/nav-entries.ts`, `src/features/theme/theme-toggle.tsx`, `src/features/shell/sign-out.tsx`. Le test `tests/coque/palette-registry.test.ts` enregistre une entrée « Test » sans toucher à ces fichiers ; `npm test` passe (contrat 23).
- [ ] `/profil` : section « Thème » avec trois boutons radio Clair, Sombre, Système ; « Système » est coché pour un compte neuf. Choisir « Sombre » : l'écran passe en sombre tout de suite et « Thème enregistré. » s'affiche sous les radios. Dans la table `user`, la colonne `theme` de `admin@exemple.fr` vaut `sombre` (contrat 24).
- [ ] Dans un autre navigateur (ou une fenêtre privée), se connecter avec `admin@exemple.fr` : `/accueil` est sombre dès l'affichage, sans passage par le clair. Avec le cookie de session de ce navigateur, `curl -s http://localhost:3000/accueil -H 'cookie: better-auth.session_token=<valeur>' | head -c 300` montre `<html lang="fr" class="dark" data-theme="sombre"` avant tout `<script>` (contrats 24 et 26).
- [ ] Refus : dans la table `user`, mettre `theme = 'clair'` pour `admin@exemple.fr`, puis la même commande `curl` : `<html lang="fr" data-theme="clair"` sans `class="dark"`. Remettre `sombre` : la classe revient (contrat 26).
- [ ] Refus de l'API : avec le cookie d'administrateur, `curl -X PATCH http://localhost:3000/api/theme -H 'content-type: application/json' -H 'cookie: better-auth.session_token=<valeur>' -d '{"theme":"violet"}'` répond `400` `"error":"theme_inconnu"` et la colonne `theme` ne change pas ; sans cookie, `401`. Avec `-d '{"theme":"systeme"}'` : `200`, la colonne vaut `systeme`.
- [ ] Avant connexion (fenêtre privée, ou après « Se déconnecter ») : `/connexion` suit le réglage de l'appareil. Avec macOS en mode sombre (ou les outils de développement, « Émuler prefers-color-scheme: dark ») la page est sombre ; en mode clair, elle est claire. Un compte en « Système » se comporte pareil une fois connecté (D17).
- [ ] « Se déconnecter » dans le pied de la barre latérale : arrivée sur `/connexion` ; `/accueil` renvoie vers `/connexion`. Se reconnecter, ⌘K, taper `déco`, Entrée : même résultat (D16).
- [ ] Connecté en membre (Inès), ouvrir `/parametres` : arrivée sur `/parametres/journal` ; sous le titre « Paramètres », la sous-navigation n'a qu'un onglet, « Journal des envois », souligné en violet. Taper `/parametres/comptes`, `/parametres/cabinet`, `/parametres/modeles`, `/parametres/envoi-test` dans la barre d'adresse : chacun renvoie vers `/accueil` (CRM-30, contrat 16).
- [ ] Connecté en administrateur, `/parametres` : arrivée sur `/parametres/comptes` ; cinq onglets « Comptes, Cabinet, Modèles d'emails, Journal des envois, Envoi de test », l'onglet de la section ouverte souligné en violet et en texte plein, les autres en gris. Cliquer « Modèles d'emails » : le soulignement suit (CRM-30).
- [ ] Téléphone (contrat 25) : fenêtre à 375 px de large (outils de développement, « iPhone SE » ou dimension personnalisée 375 × 812). Ouvrir tour à tour `/connexion`, `/reinitialisation`, `/invitation/lien-de-test` (« Lien invalide »), `/accueil`, `/profil`, `/parametres/comptes`, `/parametres/modeles`, `/parametres/journal` : aucune page ne défile horizontalement (pas de barre en bas, `document.documentElement.scrollWidth` égal à `clientWidth` dans la console), un seul titre `<h1>`, et l'action principale visible sans défiler ou après un défilement vertical : « Se connecter », « Envoyer le lien », le lien « réinitialisez votre mot de passe », le bouton de la barre latérale, « Enregistrer », « Inviter », « Modifier Invitation », « Filtrer ».
- [ ] Toujours à 375 px sur `/accueil` : la barre latérale n'est pas visible ; le bouton en haut à gauche l'ouvre en tiroir par la gauche, par-dessus la page, avec les mêmes liens et le pied (thème, déconnexion, compte). Toucher « Mon profil » : la page change et le tiroir se referme. Toucher hors du tiroir : il se referme (contrat 25).
- [ ] Refus (contrat 25) : sur `/parametres/comptes` à 375 px, les cinq onglets de Paramètres passent sur deux lignes ; aucun cadre ne défile horizontalement (les comptes sont empilés en cartes, comme en 1.2b).

## Feature 2 — Entreprises et contacts

Chaque livraison remplit uniquement sa sous-section. Les titres ne bougent pas. Ordre de série : 2.1a → 2.1b → 2.2 → 2.3 → 2.5a → 2.5b → 2.4 → 2.6a → 2.6b.

### 2.1a Entreprises : fiche, édition, historique

Préparation : `npm run db:migrate` (ajoute `company` et `audit_log`), `admin@exemple.fr` / `MotDePasse-Recette-1` créé (voir 1.2a), un membre actif (Inès, voir 1.2b), puis `npm run dev`. Écrans : `http://localhost:3000/entreprises` (liste) et `http://localhost:3000/entreprises/<id>` (fiche). Les appels serveur se font avec le cookie de session (`-H 'cookie: better-auth.session_token=<valeur>'`) et `-H 'content-type: application/json'`. Le mot « historique » désigne la colonne de droite de la fiche.

- [ ] Connectée en membre (Inès), la barre latérale a un groupe « Objets » avec l'entrée « Entreprises » ; la cliquer ouvre `/entreprises` : titre « Entreprises », un seul bouton plein « Nouvelle entreprise », un tableau (Raison sociale, Type, Ville, Responsable, Modifiée le) ou, sans entreprise, « Aucune fiche pour l'instant… » ; pied « N entreprises ». Repliée (48 px), la barre garde l'icône d'immeuble d'« Entreprises » avec son infobulle.
- [ ] Cliquer « Nouvelle entreprise » : dialogue à cinq champs (Raison sociale, Type, SIREN, Site web, Responsable pré-rempli avec Inès). Saisir `Banque Solveige`, Type « Client », puis « Créer » (ou ⌘↵) : la fiche `/entreprises/<id>` s'ouvre avec le titre « Banque Solveige », le badge « Entreprise », la ligne « Créée le <date> · modifiée le <date> · responsable : Inès Roux », et trois colonnes côte à côte à 1280 px : « Liens » (à gauche, « Aucun lien pour l'instant… »), « Champs » et « Adresse » (au centre), « Historique » (à droite) avec « Fiche créée · Inès Roux · <date>, <heure> » (contrat 1).
- [ ] Revenir sur `/entreprises` : « Banque Solveige » est la première ligne, colonne Type « Client », Responsable « Inès Roux », date du jour ; le pied compte une entreprise de plus (contrat 1, tri par dernière modification).
- [ ] Sur la fiche, Type → « Prospect », Conditions de paiement → « 60 jours », Rue `12 rue de la Paix` puis Tab, Code postal `75002` puis Tab, Ville `Paris` puis Entrée, Email de facturation `Compta@Solveige.fr` puis Entrée. Recharger : les six valeurs sont relues (l'email en minuscules `compta@solveige.fr`). L'historique, groupé sous la date du jour, montre du plus récent au plus ancien « Email de facturation : vide → compta@solveige.fr », « Ville : vide → Paris », « Code postal : vide → 75002 », « Rue : vide → 12 rue de la Paix », « Conditions de paiement : 30 jours → 60 jours », « Type : Client → Prospect », puis « Fiche créée », chacune avec « Inès Roux · <date>, <heure> » (contrat 2). Sur `/entreprises`, la fiche est remontée en tête.
- [ ] Refus (contrat 4) : « Nouvelle entreprise », Type choisi, Raison sociale vide, « Créer » : le dialogue reste ouvert, « « Raison sociale » est obligatoire. » (11 px, couleur danger) sous le champ, qui reçoit le focus ; 121 caractères : « « Raison sociale » dépasse 120 caractères. » ; 120 caractères passent. Rien n'est créé dans les deux premiers cas. Même règle par le serveur : `curl -X POST http://localhost:3000/api/entreprises … -d '{"name":"","type":"client"}'` répond `400` avec `"fields":{"name":"« Raison sociale » est obligatoire."}`.
- [ ] Refus (contrat 4) : dans le dialogue, SIREN `12345678` (huit chiffres) : « Le SIREN doit contenir neuf chiffres. » sous le champ, rien n'est créé. SIREN `123 456 789` avec une raison sociale `Siren Espaces` : créée ; la fiche montre le SIREN `123456789` (espaces retirés) et `curl http://localhost:3000/api/entreprises/<id> …` rend `"siren":"123456789"`.
- [ ] Refus (contrat 4, D19) : « Nouvelle entreprise » `Seconde` avec le SIREN `123456789` déjà porté par « Siren Espaces » : encadré rouge « Le SIREN 123456789 est déjà porté par « Siren Espaces ». » avec un lien « Ouvrir la fiche » qui mène à la fiche de Siren Espaces ; rien n'est créé. En SQL sur `crm`, `UPDATE company SET archived_at = now() WHERE name = 'Siren Espaces';` puis recommencer : même refus, message « … porté par « Siren Espaces » (fiche archivée). » ; l'appel serveur répond `409` `"error":"valeur_deja_portee"`. Remettre `archived_at = NULL` ensuite.
- [ ] Refus (contrat 5) : `curl -X POST http://localhost:3000/api/entreprises … -d '{"name":"Hors liste","type":"fournisseur"}'` répond `400` `"fields":{"type":"Valeur hors liste pour « Type »."}` ; avec `-d '{"name":"Hors liste","type":"client","paymentTerms":"90_jours"}'` : `400` sur `paymentTerms`. Les sélecteurs de la fiche ne proposent que les quatre types (Prospect, Client, Partenaire, Société de portage) et les cinq conditions (À réception, 30 jours, 30 jours fin de mois, 45 jours fin de mois, 60 jours).
- [ ] Refus (CRM-33, D21) : en SQL sur `crm`, `UPDATE company SET archived_at = now() WHERE name = 'Banque Solveige';` puis `curl -X PATCH http://localhost:3000/api/entreprises/<id> … -d '{"type":"client"}'` répond `409` `"error":"fiche_archivee"` ; l'historique n'a pas bougé ; la liste `/entreprises` ne montre plus Banque Solveige. Remettre `archived_at = NULL` : elle revient.
- [ ] Refus (contrat 15) : `curl -X PATCH http://localhost:3000/api/objets/company/<id>/historique … -d '{"oldValue":"x"}'` et `curl -X DELETE http://localhost:3000/api/objets/company/<id>/historique …` répondent tous deux `405` avec l'en-tête `allow: GET` ; `curl http://localhost:3000/api/objets/company/<id>/historique …` rend les entrées, chacune avec `"author":{"name":"…"}` et `"createdAt"` non vides (CRM-36). Sans cookie : `401` ; avec `/api/objets/inconnu/<id>/historique` : `404`.
- [ ] Refus (CRM-33, contrat 33 préparé) : `npm test` passe, dont `tests/objets/mecanismes-sans-objet.test.ts` ; écrire le mot `company` dans un commentaire de `src/features/objects/service.ts` et relancer `npx vitest run tests/objets/mecanismes-sans-objet.test.ts` : le test échoue en nommant le fichier. Retirer le mot. `tests/objets/registre.test.ts` enregistre un objet « Objet de test » et obtient ses champs et l'adresse de sa fiche sans toucher aux fichiers des mécanismes.
- [ ] Téléphone (contrat 25 de la feature 1) : fenêtre à 375 px, `/entreprises` puis une fiche : aucun défilement horizontal, un seul titre `<h1>` (« Entreprises », puis la raison sociale), le bouton « Nouvelle entreprise » visible sans défiler ; la liste ne garde que Raison sociale et Modifiée le ; la fiche empile Liens, Champs, Adresse, Historique en une colonne (D9 ; la mise en cartes complète de la liste arrive en 2.5a).
- [ ] Amorce du testeur : `.pilot/amorce-recette.js` crée « Banque Solveige », « Assurances Vaubourg » et « Groupe Ferrandi » (types client, prospect, partenaire) ; relancée, elle n'en crée pas de nouvelles (SIREN déjà portés, refus 409 ignorés) et la liste en compte toujours trois.

### 2.1b Recherche dans la palette

Préparation : base migrée (rien de nouveau en 2.1b), les trois entreprises de l'amorce (Banque Solveige, Assurances Vaubourg, Groupe Ferrandi), puis créer « ACME SAS » (type Client, SIREN `421 100 999`) par « Nouvelle entreprise » ou par `curl -X POST http://localhost:3000/api/entreprises -H 'content-type: application/json' -H 'cookie: better-auth.session_token=<valeur>' -d '{"name":"ACME SAS","type":"client","siren":"421100999"}'`. `npm run dev`, connecté en membre (Inès), sur n'importe quelle page de l'application (`http://localhost:3000/accueil`). « La palette » désigne la fenêtre ouverte par ⌘K (Ctrl+K hors macOS) ou par le bouton « Rechercher ⌘K » en haut à droite.

- [ ] Palette ouverte, taper `acm` : après un court instant, un groupe « Résultats » apparaît en tête, avant « Navigation » et « Actions », avec la ligne « ACME SAS » précédée de l'icône d'immeuble (celle d'« Entreprises » dans la barre latérale) et suivie de « Client » en gris ; la ligne est présélectionnée (surlignée). Entrée : la palette se ferme et la fiche `/entreprises/<id>` de ACME SAS s'ouvre (contrat 3).
- [ ] Rouvrir la palette, taper `me s` : « ACME SAS » est proposée et présélectionnée (sous-chaîne de la raison sociale, insensible à la casse) ; effacer et taper `421 100 999` puis `421100999` : « ACME SAS » est proposée les deux fois (SIREN, avec ou sans espaces). Taper `solv` : « Banque Solveige » est proposée (contrat 3).
- [ ] Taper `acmé` (avec accent) : « ACME SAS » est proposée (la saisie est ramenée sans accent). Créer une entreprise `Acmé Éditions` puis taper `acmé` : les deux sont proposées ; taper `acme` : seule « ACME SAS » est proposée (limite connue, dite dans la PR : les colonnes ne sont pas normalisées en base).
- [ ] Les entrées existantes restent : taper `para` puis Entrée ouvre Paramètres ; `sombre` puis Entrée bascule le thème, `clair` le remet (contrat 22 de la feature 1). Quand une saisie trouve à la fois une fiche et une entrée, le groupe « Résultats » précède toujours les autres.
- [ ] Refus (CRM-37) : taper `ac` (deux caractères) : aucun groupe « Résultats », seules les entrées qui correspondent restent (« Aller à Accueil ») ; dans l'onglet Réseau des outils de développement, aucun appel `/api/recherche` n'est parti. Ajouter le `m` : un seul appel `/api/recherche?q=acm` part, environ 200 ms après la dernière frappe (taper vite `acme` ne provoque qu'un appel, `q=acme`).
- [ ] Refus (CRM-39) : taper `zzz` : la palette affiche « Aucun résultat. » et aucune ligne ; Entrée ne fait rien (la palette reste ouverte, l'adresse ne change pas) ; Échap la ferme.
- [ ] Refus (CRM-38, D21) : en SQL sur `crm`, `UPDATE company SET archived_at = now() WHERE name = 'ACME SAS';` puis taper `acm` et `421100999` : « Aucun résultat. » les deux fois ; l'appel serveur `curl 'http://localhost:3000/api/recherche?q=acm' -H 'cookie: better-auth.session_token=<valeur>'` rend `{"results":[]}`. Remettre `archived_at = NULL` : elle revient.
- [ ] Refus de l'API (CRM-38, D24) : `curl -i 'http://localhost:3000/api/recherche?q=acm'` sans cookie répond `401` `"error":"non_authentifie"` ; avec le cookie, `?q=ac` et `?q=` répondent `400` `"error":"requete_invalide"` avec le message « La recherche attend de 3 à 120 caractères. » ; `?q=` suivi de 121 lettres : `400` ; `?q=acm` : `200` avec `"results":[{"type":"company","id":"<id>","title":"ACME SAS","subtitle":"Client","href":"/entreprises/<id>"}]`. La réponse ne dépasse jamais 20 résultats (créer 21 entreprises « Lot 1 » à « Lot 21 » et chercher `lot` : 20 lignes).
- [ ] Refus (idiome : rien d'avalé en silence) : dans les outils de développement, bloquer les requêtes vers `/api/recherche` (ou arrêter le serveur), taper `acm` : la palette affiche en rouge « La recherche n'a pas répondu. » sous le champ, sans « Aucun résultat. » ; rétablir et modifier la saisie : le message disparaît et « ACME SAS » revient.
- [ ] Téléphone (contrat 25 de la feature 1) : fenêtre à 375 px, `/accueil`, ouvrir la palette par le bouton « Rechercher », taper `acme` : la ligne « ACME SAS · Client » tient dans la largeur (texte tronqué par des points de suspension si besoin), aucun défilement horizontal (`document.documentElement.scrollWidth` égal à `clientWidth`), un seul `<h1>` sur la page une fois la palette fermée.
- [ ] Contrat 23 de la feature 1 toujours vrai : `npm test` passe, dont `tests/coque/palette-registry.test.ts` (une entrée enregistrée par un module de test) et le nouveau `tests/coque/palette-sources.test.ts` (une source de résultats enregistrée par un module de test, exposée par le registre, jamais appelée sous trois caractères, résultats rangés par rang de source). Aucun de ces tests ne modifie `src/features/shell/palette/*`.

### 2.2 Personnes et profil contact

Préparation : `npm run db:migrate` (ajoute `person`, `person_email` et `contact_profile`), les trois entreprises de l'amorce (Banque Solveige, Assurances Vaubourg, Groupe Ferrandi), connecté en membre (Inès), puis `npm run dev`. Écrans : `http://localhost:3000/personnes` (liste), `http://localhost:3000/personnes/<id>` (fiche), `http://localhost:3000/entreprises/<id>` (fiche entreprise, colonne « Liens »). Les appels serveur se font avec le cookie de session (`-H 'cookie: better-auth.session_token=<valeur>'`) et `-H 'content-type: application/json'`. « L'historique » désigne la colonne de droite de la fiche ; « la section Profil contact » la carte sous les champs, au centre.

- [ ] La barre latérale, groupe « Objets », montre « Entreprises » puis « Personnes » (dans cet ordre) ; cliquer « Personnes » ouvre `/personnes` : titre « Personnes », un seul bouton plein « Nouvelle personne », un tableau (Nom complet, Profils, Responsable, Modifiée le) ou « Aucune fiche pour l'instant… » ; pied « N personnes ».
- [ ] « Nouvelle personne » : dialogue à cinq champs (Prénom, Nom, Email principal, Entreprise, Poste), décision 7. Saisir `Camille` / `Faure` seulement, « Créer » : la fiche s'ouvre, titre « Camille Faure », badges « Personne » et « Profils : Aucun », trois colonnes à 1280 px : « Liens » (groupe « Entreprise », « Aucune fiche liée. »), « Champs » puis « Profil contact » (« Cette personne n'a pas de profil contact. » et un bouton « Ajouter un profil contact »), « Historique » (« Fiche créée »). Revenir sur `/personnes` : « Camille Faure » en tête, colonne Profils « Aucun » (contrat 8).
- [ ] Sur la fiche de Camille, « Ajouter un profil contact » puis choisir « Assurances Vaubourg » dans le sélecteur « Entreprise » : le badge passe à « Profils : Contact », la section montre Entreprise et « Rôle dans la décision : Non précisé » (le poste s'édite dans « Champs », champ « Poste »), la colonne « Liens » montre « Assurances Vaubourg » sous « Entreprise », l'historique dit « Profils : Aucun → Contact » et « Entreprise : vide → Assurances Vaubourg ». Sur `/personnes`, la colonne Profils dit « Contact » (contrat 8). `curl http://localhost:3000/api/personnes/<id> …` rend `"profiles":"contact"` et `"companyId":"<id de Vaubourg>"` ; la liste `/api/personnes` aussi (valeur exposée pour le filtre de 2.5a).
- [ ] Sur la fiche de Camille, « Email principal » `Camille.Faure@Vaubourg.fr` puis Entrée : relu `camille.faure@vaubourg.fr` ; « Autres emails » `camille@perso.fr, c.faure@autre.fr` puis Entrée : relu `c.faure@autre.fr, camille@perso.fr` (ordre alphabétique), historique « Autres emails : vide → c.faure@autre.fr, camille@perso.fr ». Palette ⌘K : `faure@vaub`, `camille@perso`, `mille fau` proposent chacune « Camille Faure » avec l'icône de « Personnes » et « Assurances Vaubourg » en gris ; Entrée ouvre sa fiche (contrat 8).
- [ ] Contrat 6 : sur `/entreprises/<id de Banque Solveige>`, colonne « Liens », groupe « Contacts » (« Aucune fiche liée. »), bouton secondaire « Ajouter une personne » : le dialogue « Nouvelle personne » s'ouvre avec « Banque Solveige » déjà choisie dans « Entreprise » (le sélecteur ne propose que des entreprises actives) ; saisir `Jean` / `Dupont` / `jean.dupont@banque-solveige.fr` / Poste `DSI`, « Créer » : la fiche de Jean s'ouvre, « Profils : Contact », « Banque Solveige » sous « Entreprise », « Poste » = `DSI` dans « Champs », rôle « Non précisé » dans la section Profil contact. Revenir sur la fiche de Banque Solveige : « Jean Dupont » est sous « Contacts » ; le cliquer rouvre sa fiche. Régler le rôle sur « Décideur » : l'historique dit « Rôle dans la décision : Non précisé → Décideur ». Le même appel par l'API fonctionne : `curl -X POST http://localhost:3000/api/personnes … -d '{"firstName":"Paul","lastName":"Martin","companyId":"<id de Solveige>","jobTitle":"DAF"}'` → `201`, la fiche liste deux contacts. Le libellé du bouton est « Ajouter une personne » (générique, tiré du registre), pas « Ajouter un contact » : dit dans la PR.
- [ ] Contrat 7 (hors activités) : sur la fiche de Jean, sélecteur « Entreprise » → « Groupe Ferrandi » : la colonne « Liens » montre « Groupe Ferrandi », l'historique dit « Entreprise : Banque Solveige → Groupe Ferrandi » ; `/entreprises/<id de Solveige>` ne le liste plus sous « Contacts » (« Aucune fiche liée. »), `/entreprises/<id de Ferrandi>` le liste. Recharger la fiche de Jean : entreprise et rôle relus.
- [ ] Refus (contrat 9, D19) : « Nouvelle personne » `Jeanne` / `Dupont` / `Jean.Dupont@Banque-Solveige.fr` (casse différente), « Créer » : encadré rouge « L'adresse jean.dupont@banque-solveige.fr est déjà portée par « Jean Dupont ». » avec un lien « Ouvrir la fiche » vers Jean ; rien n'est créé. Même refus avec une adresse posée en « Autres emails » d'une autre personne (`camille@perso.fr`) : le message nomme « Camille Faure ». En SQL sur `crm`, `UPDATE person SET archived_at = now() WHERE last_name = 'Dupont';` puis recommencer : « … par « Jean Dupont » (fiche archivée). » ; l'appel serveur répond `409` `"error":"valeur_deja_portee"` avec `existingId`. Remettre `archived_at = NULL`.
- [ ] Refus (contrat 10) : dans le dialogue, Email principal `jeanne.dupont@` : « Cette adresse n'est pas valide. » sous le champ, qui garde le focus, rien n'est créé ; par le serveur, `-d '{"firstName":"A","lastName":"B","email":"a@"}'` → `400` `"fields":{"email":"Cette adresse n'est pas valide."}`. Sur une fiche, « Autres emails » `pas-une-adresse` puis Entrée : « Cette adresse n'est pas valide : pas-une-adresse. » sous le champ et la valeur enregistrée revient.
- [ ] Refus (contrat 10) : `curl -X PATCH http://localhost:3000/api/personnes/<id de Camille>/profil-contact … -d '{"jobTitle":"DSI"}'` sur une personne **sans** profil → `400` `"fields":{"companyId":"« Entreprise » est obligatoire."}` ; `-d '{"companyId":"<id>","decisionRole":"pdg"}'` → `400` « Valeur hors liste pour « Rôle dans la décision ». » ; `POST /api/personnes` avec `"jobTitle"` sans `"companyId"` → `400`, aucune personne créée. En SQL, `UPDATE company SET archived_at = now() WHERE name = 'Groupe Ferrandi';` : le sélecteur « Entreprise » de la section Profil contact ne propose plus « Groupe Ferrandi » ; `PATCH …/profil-contact -d '{"companyId":"<id de Ferrandi>"}'` → `409` `"error":"entreprise_archivee"`. Remettre `archived_at = NULL`.
- [ ] Refus (CRM-41) : `curl -X PATCH http://localhost:3000/api/personnes/<id> … -d '{"profiles":"contact"}'` → `400` `"error":"champ_derive"` « « Profils » se déduit des profils attachés et ne se saisit pas. » ; `-d '{"name":"Autre"}'` → `400` (nom complet calculé) ; `-d '{"jobTitle":"DSI"}'` sur une personne sans profil → `400` `"fields":{"companyId":"« Entreprise » est obligatoire."}` (les clés du profil passent par le profil contact, qui exige l'entreprise) ; avec `"companyId"` en plus → `200` et le profil est créé. Sur la fiche, les champs « Nom complet » et « Profils » sont en lecture seule.
- [ ] Accès et inconnues (D24) : sans cookie, `/api/personnes`, `POST`, `PATCH` et `/profil-contact` répondent `401` ; `/api/personnes/00000000-0000-4000-8000-000000000000` et `/api/personnes/abc` répondent `404` `"error":"fiche_introuvable"` ; `http://localhost:3000/personnes/abc` affiche l'écran « 404 », jamais une erreur serveur.
- [ ] Téléphone (contrat 25 de la feature 1) : fenêtre à 375 px, `/personnes` puis la fiche d'un contact au nom long rattaché à une entreprise au nom long : aucun défilement horizontal, un seul `<h1>`, le bouton « Nouvelle personne » visible sans défiler ; la fiche empile Liens, Champs, Profil contact, Historique en une colonne ; le nom de l'entreprise dans le sélecteur est tronqué par des points de suspension.
- [ ] Amorce du testeur : `.pilot/amorce-recette.js` crée « Claire Morvan » (Banque Solveige, DSI, décideur), « Julien Tessier » (Banque Solveige, acheteur, une autre adresse), « Sofia Benali » (Assurances Vaubourg, utilisateur) et « Marc Ferrandi » (Groupe Ferrandi) ; relancée, elle n'en crée pas de nouvelles et la liste en compte toujours quatre ; la fiche de Banque Solveige liste deux contacts.
- [ ] Contrat 33 préparé : `npm test` passe, dont `tests/objets/mecanismes-sans-objet.test.ts` (la colonne des liens charge les fiches liées sans citer `person` ni `company`) et `tests/personnes/liens.test.ts`.

### 2.3 Fil d'activité, tâches, bannière

Préparation : `npm run db:migrate` (ajoute la table `activity`), les trois entreprises et les quatre personnes de l'amorce, connecté en membre (Inès), puis `npm run dev`. Écran : `http://localhost:3000/entreprises/<id>` (fiche entreprise). Le « fil » est la section « Fil d'activité », sous les trois colonnes de la fiche ; le « composeur » est le cadre en haut du fil (les boutons Note / Appel / Réunion / Tâche) ; les « puces » sont la ligne de filtres sous le composeur. Les appels serveur se font avec le cookie de session (`-H 'cookie: better-auth.session_token=<valeur>'`) et `-H 'content-type: application/json'`.

**Écart connu à vérifier tel quel** : le fil et la bannière sont montés sur la fiche **entreprise** seulement. La fiche personne est composée par un fichier hors du périmètre de cette livraison ; les activités écrites sur une personne se vérifient donc par le fil de son entreprise et par l'API, comme dit ci-dessous.

- [ ] Sur `/entreprises/<id de Banque Solveige>`, la section « Fil d'activité » est présente sous les colonnes, avec le composeur, les puces (« Tout », « Notes », « Appels », « Réunions », « Tâches », « Changements », « Emails », chacune suivie de son compteur) et, groupée sous la date du jour de création, l'entrée « Fiche créée » (type « Changement », auteur, heure).
- [ ] Contrat 11 : dans le composeur, laisser « Note » choisi, écrire `Le client valide le renouvellement.` et cliquer « Enregistrer » : l'entrée apparaît en tête du groupe du jour, avec la ligne grise « Note · Inès Roux · <date>, <heure> ». Choisir « Appel », écrire `Appel de suivi, 12 minutes.`, « Enregistrer » : deuxième entrée, type « Appel ». Choisir « Tâche », titre `Envoyer la proposition`, échéance dans une semaine, responsable déjà rempli avec Inès, « Enregistrer » : troisième entrée avec une case à cocher, « Tâche · échéance <date> · Inès Roux ».
- [ ] Contrat 11 (activité d'un contact) : le fil de Banque Solveige montre aussi les activités de ses contacts, avec le nom de la personne en lien cliquable — l'amorce en pose une sur « Claire Morvan » (« Préfère être appelée le matin. ») et une sur « Julien Tessier » (appel de suivi). Cliquer le nom ouvre la fiche de la personne. Écrire une activité sur une personne par l'API : `curl -X POST http://localhost:3000/api/objets/person/<id de Claire>/activites … -d '{"type":"note","body":"Note de recette."}'` → `201`, et elle apparaît dans le fil de Banque Solveige sous « Claire Morvan » après rechargement.
- [ ] Contrat 14 : cliquer la puce « Notes » : seules les notes restent affichées, y compris celle du contact ; « Changements » ne montre que les entrées d'historique (« Fiche créée », « Type : … → … ») ; « Tout » les remet toutes. Les compteurs des puces suivent le contenu du fil (« Tout » porte le total, « Notes » le nombre de notes) et changent dès qu'une activité est ajoutée.
- [ ] Contrat 12 : ajouter une tâche `Relancer la proposition` avec l'échéance d'**hier** : la bannière « 1 tâche échue. » apparaît en haut du contenu, au-dessus des colonnes, bordure orange à gauche. Ajouter une seconde tâche échéant **aujourd'hui** : la bannière dit toujours « 1 tâche échue. » (une échéance du jour n'est pas en retard). Cocher la case de « Relancer la proposition » : la bannière disparaît, l'entrée est barrée et sa ligne grise dit « faite ». Recharger : la case est toujours cochée, la bannière toujours absente. Décocher : la bannière revient.
- [ ] Contrat 13 : poser deux emails référençant l'entreprise, comme la feature 1 les écrira. En SQL sur `crm`, avec `<id>` celui de Banque Solveige :<br>`INSERT INTO email_log ("to", subject, body, template, status, author_id, object_type, object_id) VALUES ('contact@banque-solveige.fr', 'Proposition commerciale', '<p>…</p>', 'recette', 'envoye', (SELECT id FROM "user" WHERE email = 'ines.roux@exemple.fr'), 'company', '<id>');`<br>`INSERT INTO email_log ("to", subject, body, template, status, author_id, object_type, object_id) VALUES ('contact@banque-solveige.fr', 'Relance de rappel', '<p>…</p>', 'recette', 'echec', NULL, 'company', '<id>');`<br>Recharger la fiche : les deux entrées sont dans le fil, en lecture seule (aucune case, aucun bouton). Celle du membre affiche « Email · Envoyé · Inès Roux · <date> », **sans** le mot « automatique ». Celle du système affiche « Email · Échec · Système · <date> · automatique ». La puce « Emails » en compte deux.
- [ ] Contrat 7 (part de cette livraison) : sur la fiche de Claire Morvan, section « Profil contact », changer l'entreprise pour « Groupe Ferrandi ». Le fil de **Banque Solveige** garde les activités écrites avant le changement (elles y sont datées) ; le fil de **Groupe Ferrandi** ne les a pas. Écrire ensuite une nouvelle note sur Claire (par l'API, voir plus haut) : elle apparaît dans le fil de Groupe Ferrandi et **pas** dans celui de Banque Solveige. Remettre Claire sur Banque Solveige pour la suite de la recette.
- [ ] Contrat 16 (refus à l'écran) : dans le composeur, choisir « Tâche » sans rien saisir : le bouton « Enregistrer » est éteint, rien ne part. Saisir un titre : il s'allume. Par le serveur, `curl -X POST http://localhost:3000/api/objets/company/<id>/activites … -d '{"type":"tache","assigneeId":"<id d'Inès>"}'` → `400` `"fields":{"title":"« Titre » est obligatoire."}` ; `-d '{"type":"tache","title":"Sans responsable"}'` → `400` `"fields":{"assigneeId":"« Responsable » est obligatoire."}` ; `-d '{"type":"sms","body":"Bonjour"}'` → `400`.
- [ ] Contrat 16 (fiche archivée) : en SQL sur `crm`, `UPDATE company SET archived_at = now() WHERE name = 'Groupe Ferrandi';` puis `curl -X POST http://localhost:3000/api/objets/company/<id de Ferrandi>/activites … -d '{"type":"note","body":"Après archivage"}'` → `409` `"error":"fiche_archivee"` ; cocher une tâche de cette fiche répond `409` aussi. Remettre `archived_at = NULL`.
- [ ] Contrat 15 : `curl -X PATCH http://localhost:3000/api/objets/company/<id>/historique … -d '{"oldValue":"x"}'` → `405` avec l'entête `allow: GET` ; `curl -X DELETE …` → `405` également. À l'écran, aucune entrée « Changement » du fil ne porte de case, de bouton ni de champ modifiable.
- [ ] Accès et inconnues (D24) : sans cookie, `POST /api/objets/company/<id>/activites`, `GET` du même chemin et `PATCH /api/activites/<id>` répondent `401` ; avec cookie, une fiche inconnue (`00000000-0000-4000-8000-000000000000`) ou un identifiant qui n'est pas un UUID répondent `404` `"error":"fiche_introuvable"`, une activité inconnue `404` `"error":"activite_introuvable"` ; cocher une note (activité qui n'est pas une tâche) répond `400`.
- [ ] Téléphone, 375 px (D5) : réduire la fenêtre à 375 px sur une fiche entreprise. Deux onglets apparaissent au-dessus du contenu : « Fiche » (souligné, actif par défaut) et « Fil d'activité » suivi du nombre d'entrées. L'onglet « Fiche » montre Liens, Champs et Historique ; cliquer « Fil d'activité » montre le fil seul, et écrire une note y fonctionne. Aucun défilement horizontal, un seul `<h1>`. Élargir à 1280 px : les onglets disparaissent, la fiche et le fil sont visibles ensemble.
- [ ] Amorce du testeur : `.pilot/amorce-recette.js` pose deux notes, un appel, une réunion et une tâche échue de la veille sur les fiches de l'amorce ; relancée, elle n'en crée aucune de plus (les fils gardent le même nombre d'entrées) et n'émet aucune requête refusée dans la console.

### 2.5a Listes : filtres, tri, colonnes, édition en place, URL

_À remplir par la livraison 2.5a._

### 2.5b Vues sauvegardées et épinglées

_À remplir par la livraison 2.5b._

### 2.4 Champs personnalisés

_À remplir par la livraison 2.4._

### 2.6a Doublons et fusion

_À remplir par la livraison 2.6a._

### 2.6b Archivage et suppression protégée

_À remplir par la livraison 2.6b._
