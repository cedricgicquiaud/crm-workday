/** Emplacement du choix de thème (clair, sombre, système). Rempli par la livraison 1.3. */
export function ThemePreference() {
  return <p className="text-sm text-muted-foreground">Choix du thème : à venir (livraison 1.3).</p>;
}
