/** Emplacement du formulaire de profil (prénom, nom, mot de passe). Rempli par la livraison 1.2b. */
export function ProfileForm() {
  return <p className="text-sm text-muted-foreground">Modification du profil : à venir (livraison 1.2b).</p>;
}
